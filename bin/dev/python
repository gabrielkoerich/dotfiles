#!/usr/bin/env bash

# Install Python and usefull stuff
brew install pkg-config openssl python
# brew install poetry # Python package management tool
brew install uv # Extremely fast Python package installer and resolver, written in Rust

# Install talib
# curl -sL https://sourceforge.net/projects/ta-lib/files/ta-lib/0.4.0/ta-lib-0.4.0-src.tar.gz/download | tar xz
# cd ta-lib
# ./configure --prefix=/usr
# make

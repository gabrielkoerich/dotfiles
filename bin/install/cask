#!/usr/bin/env bash

set -euo pipefail

if ! command -v brew >/dev/null 2>&1; then
  echo "error: Homebrew not installed. Run 'just install brew' first." >&2
  exit 1
fi

# Install casks from Brewfile (no formulas in this target)
brew bundle --file Brewfile --no-brew --verbose
